Place your dashboard image here and name it `dashboard_jpg.jpg` so it can be imported by the Dashboard component.

Example path (project root):
src/assets/dashboard_jpg.jpg

After placing the file, restart the dev server if it's running so the bundler picks up the new file.